import { FormDirDirective } from './form-dir.directive';

describe('FormDirDirective', () => {
  it('should create an instance', () => {
    const directive = new FormDirDirective();
    expect(directive).toBeTruthy();
  });
});
